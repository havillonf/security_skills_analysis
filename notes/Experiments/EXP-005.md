---
id: EXP-005
tipo: experimento
data: 2026-08-22
status: amostra gerada - aguardando anotacao humana
questao: QI-1
branch: Q1
---

# EXP-005 — Piloto de anotação

> [!important] Natureza deste experimento
> É um **piloto de viabilidade, refinamento e teste do instrumento**.
> **Não estima prevalência. Não mede desempenho definitivo.** A amostra é
> deliberadamente informativa, com sobre-amostragem de situações difíceis — não
> reproduz a distribuição da população, e usá-la para estimar prevalência inflaria
> artificialmente as classes sobre-amostradas.

## Objetivo

Testar o [[Codebook]] v2.3 antes de investir no gold set (E-6). Especificamente:

1. aplicabilidade prática do codebook;
2. fronteira `SECONDARY` × `MENTION` — a única confusão que altera a prevalência;
3. regras que geram dúvida;
4. comportamento dos casos de **GRC** (R-10, [[Decision Log#D-016]]);
5. conteúdo **não inglês**;
6. casos **`mixed`**;
7. cauda linguística;
8. casos `code-review`;
9. custo humano por item;
10. custo adicional provocado por tradução;
11. concordância entre detector e **julgamento humano** de idioma — primeira medida
    de acurácia real, fechando a lacuna de [[EXP-004]];
12. necessidade de novas âncoras;
13. necessidade de revisão do instrumento antes de E-6;
14. viabilidade do critério de elegibilidade ([[Decision Log#D-022]]).

## População de origem

Representantes (`dedup_primary = 1 AND content IS NOT NULL`), **menos os 42
`file_sha` já usados na amostra de descoberta de [[EXP-002]]** → n = 1.877.939.

A exclusão é anti-circularidade: as âncoras do [[Codebook]] foram escritas a partir
daqueles casos. Reaproveitá-los mediria a regra contra os exemplos que a originaram
(achado I-3 da auditoria adversarial).

## Sinal preliminar de segurança

Não existe classificador validado, então o eixo de estratificação **não** é "classe
prevista" e sim um **sinal preliminar** ([[Decision Log#D-020]]), calculado apenas
com informação já existente.

**Léxico estrito** — 38 termos, deliberadamente **sem** os de alto ruído medidos em
[[EXP-001]] (`token` 20,69%, `audit` 16,24%, `permission` 10,07%, `sandbox`,
`safety`, `validate`):

> security · secure · vulnerab · exploit · malware · ransomware · pentest ·
> penetration test · red team · owasp · cve- · cwe- · injection · xss · csrf · ssrf ·
> sanitiz · hardening · threat model · least privilege · encrypt · decrypt ·
> cryptograph · secret scanning · credential · iam · rbac · authentication ·
> authorization · prompt injection · jailbreak · guardrail · supply chain · sbom ·
> sast · dast · forensic · incident response

**Declaração de domínio:** `domain: cybersecurity` · `domain: security` ·
`category: cybersecurity` · `category: security`

**Fórmula** (`CASE` ordenado ⇒ partição garantida por construção):

```sql
T3  WHEN domain_decl = 1 OR (fm_signal = 1 AND kw_density >= 3)
T2  WHEN fm_signal = 1 OR kw_density >= 6
T1  WHEN kw_density BETWEEN 1 AND 5
T0  ELSE
```

onde `kw_density` = nº de termos estritos distintos no corpo e `fm_signal` = termo
estrito em `name`/`description`.

**Tamanhos na população** (documentação — **não** são os `N_h` do estimador, que
virão da classificação validada em E-8):

| Tier | n | % |
|---|---|---|
| T0 | 1.192.126 | 63,48 |
| T1 | 530.918 | 28,27 |
| T2 | 67.612 | 3,60 |
| T3 | 87.283 | 4,65 |
| **soma** | **1.877.939** | **100,00** ✅ partição verificada |

> [!warning] O que o sinal preliminar **não** é
> Não é rótulo. Não é ground truth. **Não pode aparecer depois como evidência de
> validade do classificador.** O léxico estrito exclui `token`/`audit`/`permission`
> com base em [[02 - Hypotheses|H-2]], que é **hipótese não testada** — se ela
> estiver errada, T0 concentra Security Skills e a estratificação perde eficiência.

## Grupos linguísticos

L1 en · L2 zh · L3 ja+ko · L4 de/es/pt/fr/it · L5 cauda+und
([[Multilingual Strategy]] §8, valores de [[EXP-004]] v2).

`mixed` é **atributo transversal**, não grupo: uma skill em chinês com termos
técnicos ingleses continua em L2. Operacionalizado conforme [[Codebook]] R-9 —
exige ≥15% do texto na língua minoritária, ≥40 caracteres não latinos e ≥20 palavras
latinas. A versão anterior marcava `mixed` com qualquer palavra ASCII, mandava todo
o CJK para L5 e teria corrompido os pesos `N_h/N` (achado C-2).

## Seleção

Determinística. `seed = 20260822`. Pool de 900 por tier
(`ORDER BY hash(file_sha)`), detecção de idioma sobre o pool, depois:

1. **reforços dirigidos primeiro** — vagas reservadas;
2. cotas por tier × grupo no espaço restante;
3. completar se sobrar vaga.

A ordem importa: na versão anterior os dirigidos entravam por último e o corte em 50
descartava justamente os casos de fronteira `SECONDARY`/`MENTION` — o alvo declarado
do piloto (achado I-5).

## Composição da amostra (n = 50)

**Por tier:** T1 17 · T2 15 · T3 12 · T0 6
**Por grupo:** L1 25 · L2 9 · L5 8 · L4 5 · L3 3

**Por tier × grupo** — 17 células cobertas:

| | L1 | L2 | L3 | L4 | L5 |
|---|---|---|---|---|---|
| **T3** | 6 | 2 | 1 | 1 | 2 |
| **T2** | 7 | 2 | 1 | 2 | 3 |
| **T1** | 8 | 3 | 1 | 2 | 3 |
| **T0** | 4 | 2 | — | — | — |

**Idiomas presentes (10):** en 25 · zh 9 · tr 4 · ja 3 · es 2 · pt 2 · und 2 ·
fr 1 · vi 1 · ar 1

**Casos difíceis:** GRC 11 · `mixed` 12 · code-review 7 · L5 8 · com scripts 8 ·
detectores de idioma discordam 1

**Reforços dirigidos — todos atingiram a cota:** fronteira SECONDARY/MENTION 3/3 ·
GRC 4/4 · code-review 4/4 · mixed 3/3 · cauda 2/2

Metade da amostra é não inglesa, por desenho.

## Formulário — cego

> [!danger] A anotação é cega ao sinal de triagem ([[Decision Log#D-021]])
> `EXP-005_annotation_form.csv` traz apenas `case_id`, `name`, `body_chars` e os
> campos humanos **vazios**. O reading pack traz apenas nome, descrição, tamanho e
> texto. Tier, densidade, flags e motivo da seleção ficam em
> `EXP-005_strata_key.csv`, unido por `case_id` **somente depois** de a anotação
> estar fechada.
>
> Motivo: mostrar o tier antes do texto ancora o julgamento, e o viés ficaria
> **correlacionado com a probabilidade de seleção** — a única falha que
> [[Decision Log#D-014]] admite ser capaz de destruir a validade do estimador.

**Campos humanos** (todos vazios por construção; nunca preenchidos por LLM):

`human_language` · `used_translation` · `security_relevance` · `security_focus` ·
`operational_security` · `operation_level` · `security_functions` ·
`security_concerns` · `operational_capability` · `evidence` · `confidence` ·
`rule_applied` · `grc_case` · `secondary_mention_boundary` · `annotation_seconds` ·
`difficulty` · `note`

## Procedimento de anotação

1. Ler `results/EXP-005_reading_pack.md` caso a caso, **sem** abrir a chave de
   estratos.
2. Aplicar o [[Codebook]] v2.3: R-11 (cegamento) → R-1..R-7 → R-9, R-10 → R-8.
3. Preencher `results/EXP-005_annotation_form.csv`.
4. Cronometrar cada item (`annotation_seconds`) — é insumo de
   [[Decision Log#D-015]].
5. Marcar `used_translation` quando a decisão dependeu de tradução.
6. Registrar `human_language` **antes** de consultar o detector.
7. Só depois de fechar o arquivo, unir com `EXP-005_strata_key.csv` por `case_id`.

Anotador único não bloqueia ([[Decision Log#D-019]]). Se houver segundo anotador,
definir **previamente** a subamostra de dupla anotação — a literatura em ES pratica
10–30%, com moda em 20% ([[Multilingual Methodology Review]]).

## Como preencher o CSV

Exemplo completo e comentado: **`results/EXP-005_annotation_example.csv`**
(7 linhas fictícias `EX01`–`EX07`, uma por situação típica). Abra lado a lado com o
formulário real.

### Regras de formato

| Situação | Como escrever |
|---|---|
| **Lista** (`security_functions`, `security_concerns`, `operational_capability`, `evidence`) | separar por **ponto e vírgula**, sem espaço: `DETECT;ASSESS` |
| Campo **não aplicável** | deixar **vazio** (não escrever `n/a`, `null` ou `-`) |
| **Booleano** | `true` / `false` minúsculo |
| Texto **com vírgula** | entre aspas duplas: `"Acionável, mas parcial"` |
| Campo que só vale para PRIMARY/SECONDARY | vazio quando a classe for MENTION/NONE/AMBIGUOUS |

> O separador de lista é `;` porque `,` já separa as colunas do CSV. Escrever
> `DETECT, ASSESS` **quebra o arquivo**.

### Valores permitidos

- `human_language` — código ISO 639-1 (`en`, `pt`, `zh`, `ja`…), ou `mixed`, ou
  `und`. **Nunca `la`** (é artefato de detector, não idioma).
- `security_relevance` — `PRIMARY` · `SECONDARY` · `MENTION` · `NONE` · `AMBIGUOUS`
- `operation_level` — `reasoning` · `executable` · `mixed` (vazio se não se aplica)
- `security_functions` — `PREVENT` · `DETECT` · `ASSESS` · `TEST` · `RESPOND` ·
  `RECOVER`
- `operational_capability` — ver [[Codebook]] §5
- `evidence` — `description` · `body` · `bundled_artifacts`
- `confidence` — `high` · `medium` · `low`
- `difficulty` — `easy` · `medium` · `hard`
- `rule_applied` — `R-1` … `R-11`
- `security_concerns` — vocabulário livre nesta fase; anotar o termo que descreve a
  preocupação. A [[Security Taxonomy]] é candidata, não obrigatória.

### As cinco situações do exemplo

| Linha | Ensina |
|---|---|
| `EX01` | `PRIMARY` claro — remover segurança destrói a skill (R-1) |
| `EX02` | `SECONDARY` — `code-review` com seção acionável. **O caso que mais importa** |
| `EX03` | `MENTION` — o outro lado da mesma fronteira: aviso, não procedimento (R-2) |
| `EX04` | `NONE` com `used_translation: true` — traduziu para conferir |
| `EX05` | `NONE` por R-10 — GRC contratual não incide sobre sistema computacional |
| `EX06` | `AMBIGUOUS` — campos de classificação ficam **vazios**, `confidence: low` |
| `EX07` | `PRIMARY` em português — termo técnico em inglês no meio não muda o idioma |

`EX02` e `EX03` são o par mais importante: os dois falam de segurança, e a diferença
entre eles é exatamente o que decide o resultado da pesquisa. Ambos têm
`secondary_mention_boundary: true`.

### Ordem de trabalho por caso

1. Abrir o reading pack no `case_id`.
2. **Anotar `human_language` primeiro**, antes de qualquer outra coisa — é o que vai
   virar a medida de acurácia do detector.
3. Ler o texto inteiro. Cronometrar.
4. Aplicar as regras: R-1 (remoção) → R-2 (acionabilidade) → R-3 (proporção) →
   R-4/R-5 (locus e homônimos) → R-6 (scripts) → R-9/R-10 (idioma e GRC) →
   R-8 (fechamento, se ainda houver dúvida).
5. Registrar em `rule_applied` **a regra que efetivamente decidiu**.
6. Se hesitou entre `SECONDARY` e `MENTION`, marcar
   `secondary_mention_boundary: true` — mesmo tendo decidido.
7. Anotar em `note` o **porquê**, em uma frase. É isso que vira âncora nova depois.

> Não consulte `EXP-005_strata_key.csv` durante a anotação (regra R-11,
> [[Decision Log#D-021]]).

---

## Métricas calculáveis depois da anotação

- distribuição das classes na amostra (**não** é prevalência);
- taxa de `AMBIGUOUS` por tier e por grupo linguístico;
- acurácia do detector de idioma contra `human_language` — **primeira medida real**;
- tempo médio por item, global e por grupo linguístico;
- custo adicional dos casos com `used_translation`;
- frequência de cada regra em `rule_applied`;
- concordância no subconjunto GRC (reportada **à parte**);
- concentração dos casos marcados em `secondary_mention_boundary`;
- associação entre tier e classe humana — **descritiva**, jamais como validação do
  sinal preliminar.

## Critérios de conclusão

O piloto termina quando for possível responder:

- R-2 é aplicável de forma razoavelmente consistente?
- onde estão as maiores ambiguidades?
- `SECONDARY` × `MENTION` precisa de novas âncoras?
- GRC ainda gera divergência relevante? (aciona ou não [[Decision Log#D-016]] (b))
- há problemas sistemáticos por idioma?
- `mixed` está operacionalizado adequadamente?
- quanto tempo leva cada anotação, e varia por idioma?
- tradução aumenta muito o tempo?
- idioma automático concorda com o humano?
- o codebook precisa ser revisado antes de E-6?
- [[Decision Log#D-015]] pode ser decidida com base no custo observado?
- o critério de elegibilidade ([[Decision Log#D-022]]) é aplicável?

## Limitações

- **n = 50 não sustenta nenhuma estimativa.** Nem de prevalência, nem de acurácia,
  nem de concordância populacional.
- **Amostra deliberadamente enviesada** para casos difíceis. Metade é não inglesa,
  contra ~14% na população.
- **Anotador único** provável — sem kappa interavaliadores
  ([[Decision Log#D-019]]).
- O sinal preliminar depende de [[02 - Hypotheses|H-2]], não testada.
- As âncoras do codebook ainda são majoritariamente inglesas e de origem LLM
  ([[EXP-002]]) — devem ser substituídas pelas primeiras decisões humanas
  adjudicadas deste piloto.
- Elegibilidade da população em aberto ([[Decision Log#D-022]]): o frame ainda
  contém stubs e possíveis não-skills.

## Artefatos

| Arquivo | Versionado | Conteúdo |
|---|---|---|
| `scripts/build_pilot_sample.py` | sim | gerador reproduzível |
| `results/EXP-005_annotation_form.csv` | sim | formulário **cego**, campos humanos vazios |
| `results/EXP-005_annotation_example.csv` | sim | 7 linhas fictícias mostrando como preencher |
| `results/EXP-005_strata_key.csv` | sim | chave de estratos — **não abrir antes** |
| `results/EXP-005_sample_composition.json` | sim | composição, tiers, seed |
| `results/EXP-005_pilot_sample.parquet` | não (gitignored) | `case_id` ↔ `file_sha` |
| `results/EXP-005_reading_pack.md` | não (gitignored) | texto de terceiros, regenerável |

```bash
uv run --with duckdb --with py3langid --with lingua-language-detector \
  python scripts/build_pilot_sample.py
```

## Ligações

[[QI-1 Methodology]] · [[Codebook]] · [[Multilingual Strategy]] ·
[[Multilingual Methodology Review]] · [[Decision Log]] · [[EXP-001]] ·
[[EXP-002]] · [[EXP-003]] · [[EXP-004]] · [[03 - Methodology]]
